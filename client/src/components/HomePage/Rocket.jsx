import React from "react";
import { motion } from "framer-motion";

const Rocket = () => {
  return (
    <motion.div
      style={{
        y: rocketY,
      }}
    />
  );
};

export default Rocket;